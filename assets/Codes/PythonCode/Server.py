import time,random,math

print("Welcome to BETAv2 server Creation")

server = input("Enter Your Server Name: ")
while(True):
    id = input("Enter the Server Id: ")
    try:
        id = int(id)
    except:
        print("Error! Wrong ID Input")
        continue
    print("checking For Server-Locality...")
    time.sleep(random.randint(1,15))
    if(server.)
    
